import com.opengamma.tools.gradle.release.AutoVersionPlugin

apply plugin: AutoVersionPlugin

version = "3.0.0-SNAPSHOT"

dependencies {
    compile project(":basics")
    compile "org.testng:testng:${testNGVersion}"
    compile "it.unimi.dsi:fastutil:${fastutilVersion}"
    compile "org.apache.commons:commons-math3:${commonsMath3Version}"
    compile "colt:colt:${coltVersion}"
    compile "net.sourceforge.jtransforms:jtransforms:${jtransformsVersion}"

    testCompile project(":basics-testutils")
    testCompile project(":collect-testutils")
    testCompile "net.sf.opencsv:opencsv:${openCSVVersion}"
    testCompile "org.apache.poi:poi:${poiVersion}"
}
