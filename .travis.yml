sudo: false
language: java
jdk:
  - oraclejdk8
addons:
  apt:
    packages:
    - oracle-java8-installer
cache:
  directories:
    - '$HOME/.m2/repository'
install:
  - mvn validate -V -e -B
script:
  - mvn package -e -B -Dstrict
