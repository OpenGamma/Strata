description = "Calculation engine"

dependencies {
	compile project(":collect")
	compile project(":market")
	compile project(":pricer")

	compile ogAnalytics()

	testCompile project(":collect-testutils")
}

javadoc {
	options.windowTitle = "OpenGamma Strata Engine"
	title = "OpenGamma Strata Engine API Documentation"
}
